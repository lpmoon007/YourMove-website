# Secrets
.env
.env.local
.env.*.local

# Node / Next.js
node_modules/
.next/
out/
dist/
*.log
.DS_Store

# TypeScript incremental build cache
*.tsbuildinfo

# Collected early access signups, if the web server ever writes them inside the tree.
# They are email addresses and do not belong in the repository.
yourmove-signups/
*.jsonl

# The built site is not committed here. `npm run publish` builds it and pushes it to the
# `deploy` branch, which is what the web server pulls.
