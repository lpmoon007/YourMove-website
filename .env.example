# Where the signup form posts. Leave unset and it posts to signup.php, which sits next to
# the built files on the web server and appends each signup to a file outside the document
# root. Point this at a CRM or mailing list endpoint to send signups there instead.
#
# Read at build time, so rebuild after changing it.
NEXT_PUBLIC_SIGNUP_ENDPOINT=
